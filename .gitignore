node_modules/
.env
.env.local
.env.*.local
.cursor/
dist/
build/
.next/
coverage/
*.log
.DS_Store
Thumbs.db
tsconfig.tsbuildinfo

# Data (WAL/SHM excluded; .db files committed for Railway deploy)
data/*.db-shm
data/*.db-wal
app/data/*.db-shm
app/data/*.db-wal
# Pipeline working DB (local only — GitHub Actions restores from app/data/)
data/opportunity.db

# Telegram session
.telegram_session

# Pipeline secrets
pipeline/.env
